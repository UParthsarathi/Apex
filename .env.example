# Supabase — both are public (the anon key ships to the browser; RLS enforces access).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Gemini — SERVER ONLY. No NEXT_PUBLIC_ prefix, or it leaks into the client bundle.
GEMINI_API_KEY=
